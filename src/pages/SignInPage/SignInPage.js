import React from 'react'

import { AuthForm } from '../../components/AuthForm/AuthForm'

export const SignInPage = () => {
      return (
            <div>
                  <h2>Sign in</h2>

                  <AuthForm
                        type="Sign In"
                  />
            </div>
      )
}
